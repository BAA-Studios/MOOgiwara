export default class DeckManager extends Phaser.Scene {
    constructor() {
        super('deck-manager');
    }

    preload() {
        document.body.style.backgroundImage = '';
    }

    create() {
    }
}